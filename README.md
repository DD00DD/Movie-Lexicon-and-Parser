# Movie-Lexicon-and-Parser
A prolog program that creates a database working alongside a parser and lexicon to simulate a machine learning model. Asking in simple sentences will return true or false depending on whether the model finds it inside its database through using its lexicon and parser.
